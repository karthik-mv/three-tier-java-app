name: Three-Tier-App

on:
  push:
    branches:
      - main
  workflow_dispatch:

# ============================================================
# GITHUB ACTIONS PERMISSIONS
# ============================================================
permissions:
  id-token: write
  contents: read

# ============================================================
# ENVIRONMENT VARIABLES
# ============================================================
env:
  AWS_REGION: ap-south-1
  ECR_REPOSITORY: three-tier-java-app
  EKS_CLUSTER_NAME: my-cluster
  IMAGE_TAG: ${{ github.sha }}

jobs:

  # ==========================================================
  # 1. BUILD + TEST
  # ==========================================================
  build:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:

      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Build application
        run: |
          mvn clean package -DskipTests=false

      - name: Verify WAR file
        run: |
          echo "Contents of target:"
          ls -lh target/

          echo "Searching for WAR:"
          find target -name "*.war"

          if ! find target -name "*.war" | grep -q ".war"; then
            echo "ERROR: WAR file was not generated"
            exit 1
          fi

          echo "WAR file generated successfully"

      - name: Upload WAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: application-war
          path: target/*.war


  # ==========================================================
  # 2. SONARQUBE
  # ==========================================================
  sonarqube:
    name: SonarQube Analysis
    runs-on: ubuntu-latest
    needs: build

    steps:

      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: SonarQube Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

        run: |
          mvn clean verify sonar:sonar \
            -Dsonar.projectKey=three-tier-java-app \
            -Dsonar.projectName=three-tier-java-app \
            -Dsonar.host.url="$SONAR_HOST_URL" \
            -Dsonar.token="$SONAR_TOKEN"


  # ==========================================================
  # 3. DOCKER BUILD + ECR PUSH
  # ==========================================================
  docker:
    name: Build Docker Image and Push to ECR
    runs-on: ubuntu-latest

    needs:
      - build
      - sonarqube

    steps:

      # --------------------------------------------------------
      # Checkout
      # --------------------------------------------------------
      - name: Checkout source code
        uses: actions/checkout@v4


      # --------------------------------------------------------
      # GitHub context
      # --------------------------------------------------------
      - name: Show GitHub context
        run: |
          echo "Repository : ${{ github.repository }}"
          echo "Ref        : ${{ github.ref }}"
          echo "Branch     : ${{ github.ref_name }}"
          echo "Event      : ${{ github.event_name }}"
          echo "Commit     : ${{ github.sha }}"

      - name: Check AWS role secret
        env:
          AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
        run: |
          if [ -z "$AWS_ROLE_ARN" ]; then
            echo "ERROR: AWS_ROLE_ARN1 is empty or unavailable"
            exit 1
          fi
      
          echo "AWS_ROLE_ARN is available"
          echo "Role name: ${AWS_ROLE_ARN##*/}"
      - name: Check GitHub OIDC environment
        run: |
          echo "ACTIONS_ID_TOKEN_REQUEST_URL: ${ACTIONS_ID_TOKEN_REQUEST_URL:+SET}"
          echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN: ${ACTIONS_ID_TOKEN_REQUEST_TOKEN:+SET}"
      
     
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-south-1
      
      - name: Verify AWS identity
        run: aws sts get-caller-identity

      # --------------------------------------------------------
      # Login to ECR
      # --------------------------------------------------------
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
